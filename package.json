{
  "name": "line-counter",
  "version": "1.0.0",
  "description": "Lines of Code Counter for multiple languages",
  "scripts": {
    "start": "live-server --port=3000 --open=index.html"
  },
  "devDependencies": {
    "live-server": "^1.2.2"
  }
}
